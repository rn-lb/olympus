{
  "name": "olympus-vietnamese-translator-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Discord bot that translates messages into Vietnamese on request or in configured channels.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/translator.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "discord.js": "^14.18.0",
    "dotenv": "^16.4.7"
  }
}
